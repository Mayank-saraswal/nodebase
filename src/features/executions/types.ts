/**
 * Compatibility re-export.
 * Canonical module is `./types/index` (directory package).
 * Kept so existing relative imports of `./types` resolve cleanly if both forms appear.
 */
export * from "./types/index"
